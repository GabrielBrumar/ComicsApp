<resources>

    <!-- Base application theme. -->
    <style name="ComicsTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>

    </style>

    <style name="ComicsTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="ComicsTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
